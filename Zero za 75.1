#include <iostream>

#include <fstream>

#include <string>

using namespace std;

const int slowa = 800;

int main()
{
    string S;
    ifstream in("teksty.txt");
    ofstream out("wyniki.txt");
    out << "Slowa zaczyna sie i konczy na  litere 'd':" << endl;
    for(int i=0; i<slowa; i++){
        in >> S;
        if (S[0]=='d' && S[S.length()-1]=='d')
            out << S << endl;}
}
